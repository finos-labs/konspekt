---
platform: linkedin
format: feed
date: 2026-09-13
url:                      # teaser post URL, filled in once posted
announces: "usability push — teaser for the article; carousel of the type-system poster and worked example"
carousel: carousel.pdf    # 2-page PDF (graph, then poster) uploaded to LinkedIn as a document; assembled from the images below
assets:
  - file: images/type-system-poster.png
    source: docs/visuals/posters/konspekt-type-system-poster.html
    alt: konspekt type-system poster
  - file: images/graph-example.png
    source: docs/visuals/konspekt-graph-example.svg
    alt: konspekt example graph
first_comment: |
  Full write-up (article): FILL IN once the article is published
  Repo: https://github.com/finos-labs/konspekt
  Intro blog repost: https://lnkd.in/p/gspJNgc7
---

<!--
Teaser style: carousel + link-in-comment.
- Attach carousel.pdf as a DOCUMENT (paperclip), so the two pages become a
  swipeable carousel.
- Do NOT paste the article link in the body: an uploaded document and an
  auto link-preview card are mutually exclusive. Put the links in the first
  comment instead (see the first_comment field above).
- Post the article FIRST, then fill in its URL in first_comment before posting
  this teaser.
-->

konspekt now has a visual guide to its type system: one sheet that gives the single question selecting each type, plus a worked example where every box is a typed atom and every line is one edge in a single table.

The LLM that maintains the graph also got sharper about which type to propose, so the rarer types stop sitting empty.

Swipe the carousel below. Full write-up and repo in the comments.

#AI #KnowledgeGraphs #OpenSource #FINOS #konspekt
