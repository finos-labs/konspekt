pluginGroup = dev.konspekt
pluginName = konspekt
pluginVersion = 0.0.11
# Minimum supported IDE = 2026.2 (build 262); older IDEs are intentionally not supported.
pluginSinceBuild = 262
org.gradle.jvmargs = -Xmx2g -XX:+UseParallelGC
kotlin.stdlib.default.dependency = false

# --- Machine-specific settings — do NOT commit real paths here. ---
# Set these per developer in your ~/.gradle/gradle.properties (Gradle merges it),
# or pass -P on the command line. See README.md.
#   localIdePath=<your IntelliJ 2026.2 install>   # Community 2026.2 is not a
#     downloadable SDK, so the build targets your local install (local(localIdePath)).
# Run the Gradle build on a JBR/JDK 21 with JCEF (Gradle 9.7.1 needs a supported
# JDK): JAVA_HOME=<jbr21> ./gradlew buildPlugin, or set org.gradle.java.home in
# your ~/.gradle/gradle.properties (global) — not here.
