# Source excerpt — UI design artifact and diagram

Verbatim spans from the design conversation of 2026-09-14, both sides, with
elisions marked `[…]`. Selected, never paraphrased.

---

**Human:** should we create a design artifact, along with SVG for the diagram?

**Assistant:** Yes for the design document. The eleven component names we settled are currently spread across eleven noteworthy items, and nobody implementing this will reconstruct the architecture from fragments. One artifact that names the components, states which links exist and why, and records what is deliberately absent is the piece that makes the branch usable by someone else. Yes for the SVG too, with one condition. Your own principle is that artifacts are derived rather than copied, and this diagram cannot be derived: it draws components and vendor infrastructure, not graph entities, so no generator will ever reproduce it. That makes it a hand-maintained file, which is the thing the principle warns about. The way to keep it honest is to stamp it as a snapshot of this session, mark it non-normative like the whitepaper, and expect to redraw rather than patch it. What I would commit: the joined diagram, today solid and target dotted, including the storage interface and the three backends. That one supersedes every earlier version in this thread. The write-scope diagram should not go in, since you rejected the rule it depicts […]

**Human:** 1. /spec/architecture/ seems like a good place 2. design isn't really a spec; keep it non-normative 3. flat file is ok 4. component list only for now
