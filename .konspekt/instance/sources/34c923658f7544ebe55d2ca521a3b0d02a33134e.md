# Source excerpt — card contents for v1 (konspekt UI application)

Verbatim spans from the design conversation of 2026-09-14, both sides, with
elisions marked `[…]`. Selected, never paraphrased.

---

**Assistant:** Yes, and it resolves the card's awkwardness rather than working around it. The trigger is the write, not the change: a card exists only as the result of a tool call, so the moment to render one is when the agent has just proposed atoms. The tool returns the sub-graph it touched, and the human dispositions it right there, at the point of maximum context, without leaving the conversation. […] The sub-graph is what makes it worth rendering instead of listing. One hop out from the written atoms: the goal they decompose from, the concepts they mention, anything they supersede, the sibling tasks they link to.

**Human:** 1. can configure 2. same as #1 3. let's worry about read-only cards for now 4. explain this

**Assistant:** The tool result carries two things: structured content, and a pointer to the `ui://` resource. A host that negotiated the extension renders the card from the resource. A host that did not ignores the pointer and shows whatever text the result contains. Same call, same server, two renderings, and no error in either case. […] If you build the card first and write the text afterwards, the text becomes a downgraded byproduct. Writing the text first and treating the card as the richer rendering of the same content keeps both honest […] Since you have scoped cards to read-only for now, the fallback question is entirely about presentation, with no divergence in behaviour between hosts.

**Human:** ok. I know I said we can make the card contents configurable, but for now let's just work with a little table showing impacted last 10 (timewise) changed edges (atoms). atom id, atom state, timestamp. this doesn't impact the overall design, but please record this as a note
