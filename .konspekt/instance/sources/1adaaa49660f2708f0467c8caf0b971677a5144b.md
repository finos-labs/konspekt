<!-- konspekt source excerpt
     Exchange deciding how the konspekt roadmap is maintained — konspekt,
     2026-09-10 (roadmap-plan branch). Both sides included. This header is the
     only editorial text; everything below the rule is verbatim as written by
     each party, with omissions within a turn marked […]. Nothing is paraphrased. -->

---

**Claude:** [Open item #1 — how should ROADMAP.md be generated from the graph?] Local script + freshness gate […] A generator (patterned on snapshot.mjs, reusing loadInstance) emits ROADMAP.md; contributor commits it; conformance regenerates-and-diffs and fails if stale. No new workflow write permission needed. […] GitHub Action on accept — an Action regenerates and commits ROADMAP.md automatically on push-to-main […] needs workflow contents: write. […] Both: script now, Action later.

**Denis:** let's do #3, but add a noteworthy about this, with tasks for adding a workflow. The noteworthy has to mention that this design has a direct impact on how the roadmap is created/approved - an individual contributor, a designated person, a group with consensus - this defines just how complex the auto-solution becomes. What's not clear to be is how the script will be triggered

**Claude:** [Open items, still live] Roadmap open item #2 — can an outside contributor's issue become a graph proposal (issue → proposed task → human accepts), or do issues stay one-way outputs of the notifier? Roadmap open item #3 — should the roadmap poster in docs/visuals/posters/ be regenerated from the graph, or treated as a dated snapshot?

**Denis:** 1. commit 2. capture 3. no, has to be formally brought into the consensus forming syndicate 4. regenerated, given we decided to make it all dog-food dynamic.
