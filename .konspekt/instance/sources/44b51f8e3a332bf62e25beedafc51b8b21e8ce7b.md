# Source excerpt — resolve a work node from the UI

Verbatim, 2026-09-26.

---

**Human:** just realized we need to be able to resolve tasks from the UI. example there are two task-intellij-plugin* tasks - both should be resolved

**Human:** I want to add the resolve action to the UI - both standalone and plugin. I want to test in both locally.

**Assistant:** `resolve` design (mirrored in both shells):
- New write endpoint `POST /api/resolve?entity=<id>` in `server.mjs` and `ViewServer.kt` (`InstanceReader.resolveEntity`).
- Effect: flip the node's `status:` line → `resolved`; if it's `review: proposed`, also flip to `accepted` (the verb carries acceptance) reusing the existing two-way edge auto-accept. Working-tree only, no git commit, idempotent.
- Guards: nodes only (goal/investigation/experiment/topic/task/note); reject non-nodes; no-op success if already `resolved`/`abandoned`.

**Human:** both UI and the plugin work fine

**Human:** 1. yes 2. yes 3. yes 4. ok, we'll deal with this a little later; do the first 3
