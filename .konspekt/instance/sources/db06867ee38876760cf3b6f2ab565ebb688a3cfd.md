# Source excerpt — implementations thread (implementation_zero and IntelliJ plugin)

Verbatim spans from the design conversation of 2026-09-19, both sides, with
elisions marked […]. Selected, never paraphrased.

---

**Human:** three tasks: 1. add an architecture diagram (PNG) to ui-architecture.md and 2. let's sketch a practical design for a locally runnng UI - node.js running in parallel to IDE, watching changes landing under /instance and updated a floating UI - this is a implementation_zero. 3. the next implementation would be intellij plugin (also local). the enterprise implementations will come later.

**Assistant:** implementation_zero: local Node watcher + floating read-only UI […] Separate zero-dependency Node process, running beside the IDE as you said, not inside it. It reuses loadInstance() from lib/conformance.mjs — the one shared reader the checker, tools/, and the visual explorer already use, so there is no second parser. […] Full reload on change, debounced. A sync/persist writes many files at once, and nw-rename-fires-as-creation means a rename arrives as create+delete. […] SSE for the last hop. nw-poll-is-the-floor-push-is-optional puts the view's feed out of the storage interface's scope […] Read-only for zero. It reflects the working tree and never writes, so it needs no R/W API and no CAS, and propose→accept is intact because nothing writes. […] The "floating" part is a shell concern, separable from the data path.

**Assistant:** IntelliJ plugin (next, also local). Same one HTML view, rendered inside a JetBrains tool window via JCEF (the bundled Chromium). This is a third shell; the transport-adapter pattern from task-konspekt-ui-app extends to it. […] In-process and local, so it can call the R/W API directly and offer disposition and the authority verbs earlier than the web path can, while keeping propose→accept. […] Distinct from MCP Apps ui://. task-mcp-app-surface records that JetBrains is absent from the MCP Apps client matrix, so Claude's in-IDE ui:// render falls back to text.

**Assistant:** Layering: local read-only (zero) → IntelliJ plugin → enterprise, each reusing the one view. Each step reuses the one view and adds one capability (live local reflection, then local writes, then remote transport).

---

**Human:** 1. always-on-top-shell will give us better adoption 2. I think we need an /implementations directory, and under it, for each implementation /[implementation name]/docs and /[implementation name]/app etc 3. yes 4. sync
