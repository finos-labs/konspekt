<!-- Verbatim source excerpt: both sides of the exchange, quoted directly.
Omissions within a turn are marked […]. Nothing is paraphrased. -->

**Denis:** […] add an open task to the usability goal to add IntelliJ plugin distribution (zip)

**Claude:** Adding it under [[goal-usability]] as a proposed task, `task-intellij-plugin-distribution`: package the IntelliJ plugin ([[task-intellij-plugin]]) as an installable JetBrains zip so it installs from disk without building from source. Wired `decomposes` from goal-usability and `links` to task-intellij-plugin; left `review: proposed` for your acceptance.
