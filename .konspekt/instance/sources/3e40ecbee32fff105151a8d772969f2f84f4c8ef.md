# Source excerpt — three UI tasks (ASR/ADR view, related-commands, plugin pop-mode)

Verbatim, 2026-09-20.

---

**Human:** let's test this further. 1. Create a new task (ASR) - create a UI space for ASRs and ADRs. perhaps a new tab only visible if there are *any* ASRs/ADRs - should be driven by presence of stuff to show vs. engineering persona flag (which can flip back and forth). the new UI space should be a list of ASRs; clicking on each shows list of related ADRs. both ASR and ADR should be equipped by the standard details and provenance slides. 2. Another task - add "related commands" view to the details panel; perhabs the details is a 3-tab thing - details of an entity, provenance record and - if present - related commands 3. another task - experiment with an option to make the IntelliJ plugin to use pop-mode instead of tool window; make this configurable
