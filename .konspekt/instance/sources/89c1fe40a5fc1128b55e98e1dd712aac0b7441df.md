# Source excerpt — implementations thread (Electron shell decision)

Verbatim spans from the design conversation of 2026-09-19, both sides, with
elisions marked […]. Selected, never paraphrased.

---

**Assistant:** Your answer 1 is reflected: the always-on-top shell is part of zero. The design keeps the watcher+server core as zero-dependency Node (reused unchanged by the IntelliJ plugin) and layers the native window on top. The shell toolchain is the one open decision — I recommend Electron first for lowest adoption friction, Tauri if binary size matters.

**Assistant:** Open items […] Shell toolchain — Electron (recommended) or Tauri for the always-on-top window. […] The /implementations directory choice — do you want it recorded as a graph atom (a decision noteworthy), or left as repo layout only? […] artifact-implementation-zero-design — Artifact (kind: doc, location implementations/implementation-zero/docs/DESIGN.md), with a produces edge from task-implementation-zero. Confidence 0.8.

---

**Human:** 1. electron 2. yes, a graph atom 3. sync 4. update roadmap.md and sync
