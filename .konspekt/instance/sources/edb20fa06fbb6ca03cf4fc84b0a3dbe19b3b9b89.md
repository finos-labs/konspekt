<!-- konspekt source excerpt
     Exchange proposing a usability goal — konspekt, 2026-09-11. Both sides
     included. This header is the only editorial text; everything below the rule
     is verbatim as written by each party, with omissions within a turn marked
     […]. Nothing is paraphrased. -->

---

**Denis:** I keep thinking we need a UI to work on this. How about we add a goal of usability? Specifically, a UI/visualization that allows a user (human) to use konspekt artifacts visually,e.g., select a goal, see open tasks, go through them

**Claude:** konspekt already has a visual explorer. It's `visual/` […] a read-only context explorer […] Snapshot, not live […] No writes. […] What you described — "select a goal, see its open tasks, go through them" — is a task-workflow / operating interface […] The existing explorer is a read-only structure viewer, not a task-workflow UI. […] `goal-observability` already owns the read side […]. Your ask adds an operate side — a human interface to move through and act on the work.

**Denis:** [chose] New goal-usability — A distinct top-level goal: make konspekt operable by a human through an interface — navigate goal -> open tasks and work through them (review, disposition, accept/resolve). Kept distinct from goal-observability. The existing visual/ explorer becomes a starting artifact decomposed under it.
