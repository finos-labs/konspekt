<!-- konspekt source excerpt
     Exchange on why the conformance checker reports content-hash-mismatch on a
     Windows checkout — konspekt, 2026-09-10. Both sides included. This header is
     the only editorial text; everything below the rule is verbatim as written by
     each party, with omissions within a turn marked […]. Nothing is paraphrased. -->

---

**Claude:** […] **42 `content-hash-mismatch` errors — pre-existing, environmental, not mine.** *Observation:* `core.autocrlf=true`; tracked source files committed as LF are checked out as CRLF on this Windows machine. `git hash-object` normalizes and returns the matching hash (git reports them clean), but the checker reads raw disk bytes (`conformance.mjs:458`) and hashes the CRLF form. *Inference:* these are false positives on Windows and would **not** appear in CI (Linux checkout = LF). […]

**Denis:** […] 6. a noteworthy re: conformance checked - propose/accept/sync
