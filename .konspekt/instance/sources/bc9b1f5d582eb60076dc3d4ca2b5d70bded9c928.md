<!-- konspekt source excerpt
     Exchange relocating task-ingestion-mode's open sub-question — konspekt,
     2026-09-11. Both sides included. This header is the only editorial text;
     everything below the rule is verbatim as written by each party, with
     omissions within a turn marked […]. Nothing is paraphrased. -->

---

**Claude:** The four items are ingestion mechanisms — ways content flows in. The open question is not a mechanism; it's about where the operating instructions live — a packaging/distribution concern. It's attached here only because self-maintenance (mechanism #4) can't run unless the agent already has the convention loaded. […] it belongs more to the adoption/host thread (task-adoption-path, task-central-service-binding) than to ingestion. […] the pain it names is already mostly reduced. AGENTS.md is the cross-tool standard […] so you paste one file per project and every tool reads it natively […]. The remaining ambition — zero per-project paste, the host carrying it natively — is essentially task-central-service-binding.

**Denis:** yes, relocate. this has to do with both enterprise mode and vendor adoption
