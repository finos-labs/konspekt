<!-- konspekt source excerpt
     Verbatim transcript of the generic node-link ("links") founding exchange,
     konspekt, 2026-09-12. Both sides included. This header is the only
     editorial text; everything below the rule is verbatim as written by each
     party, with omissions within a turn marked […]. No included line is
     paraphrased. -->

---

**User:** "I am thinking we need a general "linked to" relationship type between nodes of any type; something like what JIRA has -having  typed links, but also "linked to""

**Claude:** "**Add one generic core edge kind for node ↔ node, symmetric, optionally weighted; keep `relates` for concepts; push JIRA-style *typed* links to persona layers.** […] **Constrain the generic link to node ↔ node, not entity ↔ entity.** This is the load-bearing decision. A link that accepts *any* type would compete with the typed cross-type edges (`mentions`, `produces`, `notes`) — people would reach for `links` instead of the specific kind, and the typed vocabulary would erode. JIRA avoids this because issues are the only linkable thing; you get the same protection by scoping the generic link to nodes. […] **The JIRA "typed links" (blocks / depends-on / duplicates) → persona layers, deferred.**"

**User:** "1. "links" is fine 2. node to node 3. confirmed 4. as-is is ok"

**User:** "1. rename 2. refresh 3. capture as a waypoint and sync"
