<!-- Verbatim source excerpt: both sides of the exchange, quoted directly.
Omissions within a turn are marked […]. Nothing is paraphrased. -->

**Denis:** can we just distribute .zip and require certain IDE version? Asking others to build it is pointless - might as well put it into plugin marketplace

**Claude:** Yes — and that's exactly the intended model. Install-from-disk of a prebuilt zip is precisely "here's the artifact, you just need a recent enough IDE." No one but the maintainer builds. […] the zip channel only makes sense as a prebuilt artifact. The maintainer builds it once locally (the local-only 2026.2 SDK means CI can't, but for a build-once-and-upload release that doesn't matter) and attaches it to a GitHub release. […] the built descriptor currently defaults to until-build="262.*", so today's zip installs on 2026.2.x only and would be refused on 2026.3+ […] clear the cap so the floor means "2026.2 or newer".
