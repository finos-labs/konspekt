# Source excerpt — implementations thread (implementation_zero app build)

Verbatim spans from the design conversation of 2026-09-19, both sides, with
elisions marked […]. Selected, never paraphrased.

---

**Human:** accept and commit, then move to acctual implementation

**Human:** kill the shell. implement stats and goals

**Assistant:** server.mjs: zero-dependency Node core. Watches the instance (fs.watch, recursive, debounced), reloads through loadInstance() in lib/conformance.mjs, and serves GET / (view), GET /api/entities (JSON), GET /events (SSE). Read-only.

**Assistant:** Stats tab (task-graph-analytics measures): four stat tiles (entities, edges, accepted %, open proposals), a "by kind" accepted-vs-proposed stacked bar per entity kind, and an "oldest unaccepted proposals" list by age.

**Assistant:** Goals tab (task-goal-task-navigation): goals list with per-goal rollup; click a goal → its decomposes subgraph rendered as a layered SVG DAG (node border = status, dashed = proposed), click a node for its detail.

**Assistant:** electron-main.mjs + package.json: the always-on-top shell (nw-electron-shell); spawns the core and loads the localhost view.

---

**Human:** looks good - commmit, atoms included. push and raise PR
