<!-- Verbatim source excerpt: both sides of the exchange, quoted directly.
Omissions within a turn are marked […]. Nothing is paraphrased. -->

**Denis:** shouldn't the zip be a part of distribution? and have init.mjs copy the zip from konspekt clone to user's IntelliJ plugin dir or just update readme to say "take zip from here and use install from disk"

**Claude:** No, the zip should not live in `distribution/`. That tree is a generated projection of the standard + adopter kit […] its whole purpose is "what an adopter conforms to," and it's regenerable/zero-binary […] No, `init.mjs` should not copy the zip into the IDE plugins dir. `init.mjs`'s entire model is "write files into the target repo" […] the IDE plugins directory is machine-global, per-OS, per-IDE-version, per-developer — outside the repo and outside its remit. […] Yes to the README approach — with one refinement: the binary's channel is the GitHub release asset […] The release asset is the distribution; docs are the pointer.

**Denis:** ok go for it. make sure the path is complete "implementations\intellij-plugin\app\build\distributions"
